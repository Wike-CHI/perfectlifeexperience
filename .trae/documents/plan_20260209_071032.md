我将执行以下计划，将您的本地数据和云函数完整部署到云开发环境：

### 1. ☁️ 部署云函数
将 `cloudfunctions` 目录下的所有业务函数上传并部署到云端，包括：
- `login` (登录)
- `wallet` (钱包)
- `coupon` (优惠券)
- `promotion` (推广)
- `rewardSettlement` (结算)
- `hello` (测试)
- `initData` (原始初始化)

### 2. 🗄️ 创建数据库集合
根据代码分析，为您自动创建以下必要的数据库集合：
- `categories` (分类)
- `products` (商品)
- `users` (用户)
- `orders` (订单)
- `coupons` (优惠券信息)
- `user_coupons` (用户领取的优惠券)
- `promotions` (推广活动)
- `wallets` (用户钱包)
- `wallet_transactions` (钱包流水)

### 3. 🔄 商品数据管理
商品数据已迁移到云端数据库，通过 `product` 云函数进行查询和管理。
- 前端通过 `src/utils/api.ts` 调用 `product` 云函数获取商品和分类数据
- 数据存储在云端的 `categories` 和 `products` 集合中
- 支持多规格商品、搜索、分类筛选等功能

### 4. ✅ 验证
部署完成后，我将检查云函数状态和数据库数据量，确保一切就绪。
