我将执行以下计划，将您的本地数据和云函数完整部署到云开发环境：

### 1. ☁️ 部署云函数
将 `cloudfunctions` 目录下的所有业务函数上传并部署到云端，包括：
- `login` (登录)
- `wallet` (钱包)
- `coupon` (优惠券)
- `promotion` (推广)
- `rewardSettlement` (结算)
- `hello` (测试)
- `initData` (原始初始化)

### 2. 🗄️ 创建数据库集合
根据代码分析，为您自动创建以下必要的数据库集合：
- `categories` (分类)
- `products` (商品)
- `users` (用户)
- `orders` (订单)
- `coupons` (优惠券信息)
- `user_coupons` (用户领取的优惠券)
- `promotions` (推广活动)
- `wallets` (用户钱包)
- `wallet_transactions` (钱包流水)

### 3. 🔄 同步本地数据 (`menu_data.json`)
检测到 `src/data/menu_data.json` 是您本地调试的真实数据，而云端的 `initData` 使用的是旧的演示数据。
我将创建一个专用的数据同步脚本（临时云函数 `uploadLocalData`），执行以下操作：
- 读取 `menu_data.json` 中的“鲜啤外带”和“增味啤”数据。
- 将其智能转换为数据库格式（保留多规格价格 `prices` 字段）。
- 自动写入到云数据库的 `categories` 和 `products` 集合中。
- **效果**：云数据库将拥有与您本地调试完全一致的最新商品数据。

### 4. ✅ 验证
部署完成后，我将检查云函数状态和数据库数据量，确保一切就绪。
