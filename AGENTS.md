# 大友元气精酿啤酒小程序 - AI 开发指南

> **项目概述**：UniApp + 腾讯云开发(CloudBase) 的精酿啤酒电商小程序  
> **支持平台**：微信小程序、H5、支付宝小程序、抖音小程序

---

## 📑 快速导航

| 章节 | 说明 |
|-----|------|
| [🚀 常用命令](#-常用命令) | 开发、构建、部署命令 |
| [🔧 环境配置](#-环境配置) | CloudBase 环境、AppID 等 |
| [🏗️ 项目架构](#️-项目架构) | 四级推广系统、佣金结构 |
| [📁 目录结构](#-目录结构) | 项目文件组织 |
| [🔐 认证流程](#-认证流程) | 静默登录机制 |
| [☁️ 云函数](#️-云函数) | 云函数架构和列表 |
| [⚠️ 重要约束](#️-重要约束) | 开发和部署限制 |
| [🐛 常见问题](#-常见问题) | 问题排查指南 |
| [📖 技能文档](#-技能文档) | AI Skills 参考 |

---

## 🚀 常用命令

### 开发命令

```bash
# 微信小程序开发
npm run dev:mp-weixin

# 生产构建 - 微信小程序
npm run build:mp-weixin

# H5 开发（用于 Web 测试）
npm run dev:h5

# H5 生产构建
npm run build:h5

# 类型检查
npm run type-check
```

### 测试命令

```bash
# 运行测试
npm run test

# 生成覆盖率报告
npm run test:coverage
```

### 构建输出

| 平台 | 输出目录 |
|-----|---------|
| 微信小程序 | `dist/build/mp-weixin/` |
| H5 | `dist/build/h5/` |

---

## 🔧 环境配置

### CloudBase 环境

| 配置项 | 值 |
|--------|-----|
| **环境 ID** | `cloud1-6gmp2q0y3171c353` |
| **微信 AppID** | `wx4a0b93c3660d1404` |
| **云函数运行时** | Node.js 16.13 |
| **配置文件** | `src/utils/cloudbase.ts` |

### 控制台入口

- **CloudBase 控制台**: https://tcb.cloud.tencent.com/dev?envId=cloud1-6gmp2q0y3171c353
- **微信开发者工具**: 见下方打开方式

### 打开微信开发者工具

**macOS:**
```bash
/Applications/wechatwebdevtools.app/Contents/MacOS/cli open --project "/path/to/project"
```

**Windows:**
```bash
"C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat" open --project "项目根目录路径"
```

---

## 🏗️ 项目架构

### 双轨推广系统（核心业务）

```
┌─────────────────────────────────────────────────────────┐
│                    总公司                                │
│                    (系统级)                              │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
   ┌─────────┐  ┌─────────┐  ┌─────────┐
   │ 一级代理 │  │ 金牌推广 │  │  铜牌   │
   │ (20%)   │  │  (20%)  │  │  (12%)  │
   └────┬────┘  └─────────┘  └─────────┘
        │
   ┌────┴────┐
   │ 二级代理 │ (12% + 8%给上级)
   │ 银牌推广 │
   └────┬────┘
        │
   ┌────┴────┐
   │ 三级代理 │ (12% + 4%×2给上级)
   │ 铜牌推广 │
   └────┬────┘
        │
   ┌────┴────┐
   │ 四级代理 │ (8% + 4%×3给上级)
   │ 普通会员 │
   └─────────┘
```

### 佣金分配规则

| 推广级别 | 自己 | 上级1 | 上级2 | 上级3 | 总计 |
|---------|------|-------|-------|-------|------|
| 一级推广 | 20% | - | - | - | 20% |
| 二级推广 | 12% | 8% | - | - | 20% |
| 三级推广 | 12% | 4% | 4% | - | 20% |
| 四级推广 | 8% | 4% | 4% | 4% | 20% |

### 升级条件

| 从 | 到 | 条件（满足其一） |
|----|----|----------------|
| 普通 | 铜牌 | 累计销售 2万元 |
| 铜牌 | 银牌 | 月销售 5万元 **或** 团队 50人 |
| 银牌 | 金牌 | 月销售 10万元 **或** 团队 200人 |

---

## 📁 目录结构

```
perfectlifeexperience/
├── src/                          # 前端源码
│   ├── pages/                    # 小程序页面
│   │   ├── index/               # 首页/产品列表
│   │   ├── product/             # 产品详情
│   │   ├── order/               # 订单相关
│   │   ├── promotion/           # 推广中心
│   │   ├── wallet/              # 钱包
│   │   └── ...
│   ├── pagesAdmin/              # 管理后台页面
│   ├── components/              # 公共组件
│   ├── utils/                   # 工具函数
│   │   ├── cloudbase.ts        # CloudBase 初始化
│   │   ├── api.ts              # API 封装
│   │   ├── database.ts         # 数据库查询
│   │   └── format.ts           # 格式化工具
│   ├── types/                   # TypeScript 类型
│   ├── constants/               # 常量定义
│   └── composables/             # Vue Composables
├── cloudfunctions/              # 云函数
│   ├── login/                   # 用户登录
│   ├── user/                    # 用户管理
│   ├── promotion/               # 推广系统（核心业务）
│   ├── order/                   # 订单管理
│   ├── wallet/                  # 钱包
│   ├── wechatpay/              # 微信支付
│   ├── admin-api/              # 管理后台 API
│   └── common/                  # 公共工具
├── admin_dash/                  # 独立管理后台
├── docs/                        # 项目文档
└── dist/                        # 构建输出
```

---

## 🔐 认证流程

小程序采用**静默登录**，无需用户手动授权：

```
App 启动
   │
   ▼
initCloudBase() ──► 检查 CloudBase 环境
   │
   ▼
checkLogin() ──► 调用 login 云函数
   │
   ▼
获取 OPENID ──► 从 wxContext.OPENID 获取
   │
   ▼
用户状态缓存 ──► 本地存储，7天有效期
   │
   ▼
API 调用 ──► 所有云函数通过 wxContext 接收 OPENID
```

> ⚠️ **重要**：永远不要信任前端传递的用户 ID，始终使用 `wxContext.OPENID` 进行认证。

---

## ☁️ 云函数

### 架构模式

所有云函数使用 **action 路由模式**：

```javascript
exports.main = async (event, context) => {
  const { action, data } = event;
  const wxContext = cloud.getWXContext();
  
  switch (action) {
    case 'actionName':
      return await handleActionName(data, wxContext);
    default:
      return { code: 400, msg: `Unknown action: ${action}` };
  }
};
```

### 标准响应格式

```javascript
// 成功响应
{ code: 0, msg: 'Success message', data: {...} }

// 错误响应
{ code: -2, msg: 'Parameter error' }
```

### 云函数列表

| 云函数 | 功能 | 关键 Actions |
|--------|------|-------------|
| **login** | 用户认证 | 获取 OPENID、创建用户 |
| **user** | 用户管理 | `loginOrUpdate`, `getUserInfo`, `updateUserInfo` |
| **promotion** | 推广系统（核心） | `bindRelation`, `calculateReward`, `getTeamMembers` |
| **order** | 订单管理 | `createOrder`, `getOrders`, `updateOrderStatus` |
| **wallet** | 钱包余额 | `getBalance`, `recharge`, `withdraw` |
| **commission-wallet** | 佣金钱包 | `getBalance`, `applyWithdraw` |
| **wechatpay** | 微信支付 | `createPayment`, HTTP 回调处理 |
| **admin-api** | 管理后台 | 订单/用户/产品/财务管理 |
| **coupon** | 优惠券 | `getCoupons`, `claimCoupon`, `useCoupon` |
| **upload** | 文件上传 | 头像上传到云存储 |

---

## ⚠️ 重要约束

### 云函数运行时

- **运行时不可变**：创建后无法更改
- 当前运行时：**Nodejs16.13**
- 需要更改运行时必须删除重建

### NoSQL 数据库限制

| 限制 | 说明 |
|-----|------|
| **无原生 JOIN** | 需要反规范化或多重查询 |
| **数组操作受限** | 数组更新需要特殊语法 |
| **事务支持有限** | 不支持跨集合事务 |

### 小程序特定限制

- **包大小限制**：小程序代码必须小于 2MB（未压缩）
- **文件存储**：使用 `wx.cloud.uploadFile()` 上传图片，不要用 base64
- **域名白名单**：所有 API 域名必须在微信小程序后台配置

---

## 🐛 常见问题

### 云函数超时
- 在 CloudBase 控制台检查执行时间
- 使用索引优化数据库查询
- 考虑异步处理长耗时操作

### 登录问题
- 验证 `src/utils/cloudbase.ts` 中的环境 ID
- 检查 7 天缓存过期逻辑
- 查看 `login` 云函数日志

### 推广计算错误
- 验证用户 `promotionPath` 格式（格式：`"parentId1/parentId2/..."`）
- 检查代理级别计算（最多 4 级深度）
- 查看业绩追踪的月份标签是否匹配

### 构建/编译错误
- 清除 `dist/` 和 `node_modules/` 后重试
- 确保代码中引用的文件路径存在
- 检查导入是否使用正确别名（`@/` 映射到 `src/`）

### 微信支付失败
- 验证商户证书和私钥
- 检查 HTTP 触发器回调 URL
- 查看支付签名验证日志

---

## 📖 技能文档

技能文档位于 `.agents/skills/` 目录，提供详细开发指导：

### 核心技能

| 技能 | 路径 | 用途 |
|-----|------|------|
| **CloudBase 指南** | `cloudbase-guidelines/SKILL.md` | CloudBase 开发必读指南 |
| **UI 设计** | `ui-design/SKILL.md` | 界面设计规范（**生成 UI 前必须阅读**） |
| **工作流规范** | `spec-workflow/SKILL.md` | 需求分析、技术设计流程 |

### 平台开发

| 平台 | 技能路径 |
|-----|---------|
| **小程序开发** | `miniprogram-development/SKILL.md` |
| **Web 开发** | `web-development/SKILL.md` |
| **云函数** | `cloud-functions/SKILL.md` |
| **CloudRun** | `cloudrun-development/SKILL.md` |

### 认证

| 场景 | 技能路径 |
|-----|---------|
| **小程序认证** | `auth-wechat-miniprogram/SKILL.md` |
| **Web 认证** | `auth-web-cloudbase/SKILL.md` |
| **Node.js 认证** | `auth-nodejs-cloudbase/SKILL.md` |
| **认证工具配置** | `auth-tool-cloudbase/SKILL.md` |

### 数据库

| 类型 | 技能路径 |
|-----|---------|
| **NoSQL (小程序)** | `cloudbase-document-database-in-wechat-miniprogram/SKILL.md` |
| **NoSQL (Web)** | `cloudbase-document-database-web-sdk/SKILL.md` |
| **MySQL (MCP)** | `relational-database-mcp-cloudbase/SKILL.md` |
| **MySQL (Web)** | `relational-database-web-cloudbase/SKILL.md` |

### AI 能力

| SDK | 技能路径 |
|-----|---------|
| **Web SDK** | `ai-model-web/SKILL.md` |
| **Node.js SDK** | `ai-model-nodejs/SKILL.md` |
| **小程序** | `ai-model-wechat/SKILL.md` |

---

## 🎯 AI 开发速查

### 开始新任务时

1. **读取技能文档** - 根据任务类型读取对应的 SKILL.md
2. **UI 任务必须先读** - `ui-design/SKILL.md`（**强制**）
3. **检查环境** - 确认 CloudBase 环境 ID 和配置
4. **遵循代码风格** - 保持与现有代码一致

### 代码规范

- 使用 **TypeScript** 类型
- 云函数使用 **action 路由模式**
- 使用 **标准响应格式** `{code, msg, data}`
- 遵循 **东方美学设计系统**（深棕色 #3D2914、琥珀金 #C9A962）
- **永远不要信任前端用户 ID**，使用 `wxContext.OPENID`

### 数据库集合

```javascript
// 主要集合
users, admins                    // 用户
products, categories             // 产品
orders, refunds                  // 订单
promotion_relations              // 推广关系
promotion_orders                 // 推广订单
reward_records                   // 奖励记录
user_wallets, commission_wallets // 钱包
coupons, user_coupons            // 优惠券
```

---

*最后更新: 2026-02-27*  
*如有疑问，参考 `CLAUDE.md` 获取更详细的项目信息*
